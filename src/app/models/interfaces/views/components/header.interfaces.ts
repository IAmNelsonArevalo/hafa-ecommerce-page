export interface ISsocialNetworks {
  name: string;
  url: string;
}

export interface IRoutesMenu {
  path: string;
  name: string;
}

export interface IUser {
  fullname: string;
  email: string;
  phone: string;
}
